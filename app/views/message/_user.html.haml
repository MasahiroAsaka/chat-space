.user_comments
  / %p.title-of-user sample
  / %p.message-of-user Good bye world!
  - current_user.groups.each do |group|
    .group
      = link_to group_messages_path(group) do
        .group__name
          = group.name
        .group__message
          メッセージはまだありません。
